# 2026-08-28 工作日志

## 项目代码级审计报告
- 生成完整审计报告：`docs/audits/2026-08-28-project-audit.md`（区别于 2026-08-26 治理审计）。
- 方法：读 Constitution/技术方案/Roadmap + 并行 4 个 Explore 代理（安全/代码质量架构/测试性能/依赖配置），并对全部 High 结论二次 grep/Read 核验。
- 关键发现（已核验）：
  - **Money VO 死代码**：`common-core/.../money/Money.java` 设计优良但源码零引用，金额用裸 `(long amountMinor, String currencyCode)` 元组（High，违反宪法）。
  - **安全缺位**：全仓无 Spring Security、无 Bean Validation、回调无验签、`/{id}/resolve` 可伪造支付成功；DB 密码硬编码 root/root。
  - **事务内同步 RPC**：`PaymentApplicationService.createPaymentIntent`（@Transactional, :59）在事务内做 `channel.charge`(:76) 与 `fulfillmentGateway.notifyPaymentSucceeded`(:87)；Feign 无任何 timeout/熔断配置。
  - **测试跑 H2**：全仓 0 个 Testcontainers `@Container`，违反宪法「Testcontainers(MySQL) 集成测试」。
  - 正向：分层纯净、状态机集中带守卫、幂等靠 DB 唯一约束、金额无浮点、无 SQL 注入、版本零漂移。
- 当前进度：Feature 001 已验收；进行中 002-payment-order-callback + 009 Observability；master 分支有大量未提交改动（末次提交 688381c, 2026-08-27）。
