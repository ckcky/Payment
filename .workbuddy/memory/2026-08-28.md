# 2026-08-28 工作记录

## 审计问题整改（用户指令：把这些问题修了；DB 密码硬编码按用户要求不改）
针对 `docs/audits/2026-08-28-project-audit.md` 的高优先级问题，实施了 4 项可验证修复（全部通过 payment-service 45 测试 + 全 reactor 编译）：
1. **P0-3 事务内同步 RPC**：新增 `PaymentPersistence`（`@Transactional` 的 insertPending/applyAndPersist），`createPaymentIntent` 中 `channel.charge` 与履约 RPC 移出 DB 事务。
2. **P0-2/5.2/5.3 超时/熔断/重试**：Feign `connect/read-timeout` + `circuitbreaker.enabled`；pom 加 `spring-cloud-starter-circuitbreaker-resilience4j`；新增 `ResilientFulfillmentGateway`（指数退避重试）+ `@Primary` 包裹 Feign 客户端（注意 `@FeignClient(primary=false)` 避免双 primary 冲突）。
3. **P1-6/F3 输入校验**：common-dto 加 `spring-boot-starter-validation`；`CreatePaymentRequest`/`ResolveRequest` 加 `@NotNull/@Positive/@Pattern`；`PaymentController` 加 `@Valid`。
4. **F2 /resolve 鉴权**：新增 `ResolveAuthorizationInterceptor`（X-Admin-Token == payment.admin.token，fail-closed）+ `WebConfig` 注册到 `/payments/*/resolve`。
- 未改：F4 DB 密码硬编码（用户明确不改）。
- 仍待办（大型/Phase 9）：Money VO 全量启用、F1 全站 Security、F5 回调签名、Nacos、Testcontainers、Schema Flyway、CI lint、Dockerfile。

## 环境：Maven 构建调用方式（重要，跨会话复用）
- `cmd.exe` 被安全策略全局禁用（Bash/PowerShell 中都不能用），`mvnw` 因缺 `maven-wrapper.jar` 无法启动。
- 可用 Maven 发行版位于 `~/.m2/wrapper/dists/apache-maven-3.9.9-bin/.../apache-maven-3.9.9`。
- 直接调用 classworlds 启动器（绕过脚本的路径问题，必须用 Windows 反斜杠路径，单引号避免 Git Bash 转义）：
  `JH='C:\Program Files\Eclipse Adoptium\jdk-25.0.1.8-hotspot'; MH='C:\Users\user\.m2\wrapper\dists\apache-maven-3.9.9-bin\4nf9hui3q3djbarqar9g711ggc\apache-maven-3.9.9'; "$JH\bin\java" -classpath "$MH\boot\plexus-classworlds-2.8.0.jar" -Dmaven.home="$MH" -Dclassworlds.conf="$MH\bin\m2.conf" -Dmaven.multiModuleProjectDirectory="C:\Users\user\Desktop\GoProj\PaymentArch" --enable-native-access=ALL-UNNAMED org.codehaus.plexus.classworlds.launcher.Launcher <args>`
- 注：`/c/...` 形式的路径传给 Windows `java` 会找不到资源；Maven classpath 的 jar 在 `boot/` 而非 `lib/`。

## 架构文档审计（用户指令：审计一下架构文档）
产出 `docs/audits/2026-08-28-architecture-doc-audit.md`。核心结论：文档「骨架/规范」一流，但「内容/现实」已系统性漂移。关键发现（均 grep/ls 复核）：
- D1 🔴 Constitution §II.3 MUST 经 ledger-service vs Roadmap/技术方案把 Ledger 延后 Phase 8（自相矛盾）。
- D2 🔴 Feature 编号错位：文档称「下一个 002 Payment Reliability」，实际 002=payment-order-callback、003=payment-reliability；roadmap 内「002」自相矛盾。
- D3/D4/D5 🔴 文档称已用 Nacos / Money VO / Spring Security，代码均零引用（Nacos=0、Money src/main=0、security pom=0）。
- D6 🟠 payment-service.md 仍描述「事务内同步 RPC / Feign 未配置」，与近期代码修复不符（滞后）。
- D7 🟠 ADR-0001 引用已不存在的 Constitution §3.1；D8 服务清单 11 vs 9 口径；D9 ./mvnw wrapper jar 缺失；D11 系统设计文档仅 2/9 篇。
- 建议 P0：D1 加 MVP 例外条款、D2 统一编号、D3/D4/D5 改标 `[目标]/Phase 9`。

## 按 Spec Kit 流程开发下一个 Feature 003-payment-reliability
用户指令：`/speckit-spec` 继续开发下一个 spec（Payment Reliability），使用 spec 开发流程，分歧/歧义点记录 ADR。
- 落地产物（docs/specs/003-payment-reliability/）：spec.md、checklists/requirements.md、plan.md、research.md、data-model.md、contracts/manual-resolution.md、quickstart.md、tasks.md；`.specify/feature.json` 已指向 003。
- 范围聚焦尚未实现的机制：超时→UNKNOWN 调度、主动查询/回调收敛、有限重试+退避+耗尽、人工收敛端点、可靠性指标/告警；复用既有 PaymentUnknownResolutionService/状态机终态吸收/002 成功回写。
- 5 个架构分歧点落到 ADR（docs/adr/，状态均为 Proposed 待 §8 确认）：0003 UNKNOWN 收敛触发（主动查询+人工）、0004 超时进 UNKNOWN 策略（默认 30s）、0005 重试模型（上限3/退避1-2-4s/耗尽→UNKNOWN）、0006 人工收敛权限+审计（FINANCIAL_AUDIT）、0007 终态冲突（迟到成功不覆盖已失败）。README 索引已更新。
- 顺带闭合了架构审计 D2（Feature 编号错位）：采用顺序编号 003-payment-reliability，与 Roadmap 阶段标签「002 Payment Reliability」解耦（已在 spec Clarifications 记录）。
- 实现前置：须先由负责人确认 ADR-0003~0007（尤其 §8.5 安全 / §8.8 状态机）。沙箱 Maven 仍不可用，验证交用户在本地 DB 环境执行。

## 验证：本项目可本地构建+测试（2026-08-28 复测）
- 构建调用：用 **PowerShell 工具**直接运行 Windows 原生 `& 'C:\Users\user\apache-maven-3.9.5\bin\mvn.cmd' <args>`（Bash 调 powershell.exe/cmd.exe 被安全策略阻断；Git Bash 下 `./mvnw`/`mvn` 因 Unix 路径 classpath 报 classworlds.Launcher 缺失，已不可用）。
- 环境：JDK 25.0.1 (Eclipse Adoptium) / Maven 3.9.5 / Windows 11；本机 `.m2` 依赖齐全，无需联网。
- 结果：`mvn compile` 13/13 模块 SUCCESS；`mvn test`（H2，无 Testcontainers）BUILD SUCCESS，约 60+ 测试类全部通过、0 失败 0 错误，JDK 25 下 Spring Boot 3.5 运行正常。
- 缺口：本机无 Docker，无法起 MySQL，真实微服务（spring-boot:run）未能在本机启动；测试层 H2 已验证核心逻辑/持久化/Spring Context。

## Feature 003 开发推进（2026-08-28 下午）
- **ADR 合并**：原 `0003~0007` 五份独立 ADR 合并为单文件 `docs/adr/0003-payment-reliability-decisions.md`（用户要求「放在一个文件」）。状态：0003/0004/0005/0007 = **Accepted**，0006（人工收敛）= **Not Implemented / Deferred**（延后 Phase 9 Risk/Security）。已删除 5 个旧文件，README 索引更新为单行。
- **架构/文档标注**：`technical-solution.md` §4.3.2 增加 ADR-0006 延后注记；`spec.md` Status→In Development、US4 标 Deferred；`tasks.md` T001 完成、Phase 6（US4）冻结、T002/T006/T007/T008/T009 完成。
- **实现 US1 MVP（超时→UNKNOWN）**：新增 `payment-service/.../application/reliability/`（ReliabilityConfig + TimeoutScanner + TimeoutScanScheduler）；`PaymentApplication` 加 `@EnableScheduling`；`application.yml` 加 `payment.reliability` 配置块（timeout 30s / scan 10s / retry 3 / query 5）；测试 `TimeoutScanTest`（4 例：超阈值→UNKNOWN、近期不误判、非 PROCESSING 跳过、二次幂等）。
- **验证**：PowerShell 调 `& 'C:\Users\user\apache-maven-3.9.5\bin\mvn.cmd' -o test -pl payment-service -am` → **BUILD SUCCESS，49 测试全过**（含新增 4 例），`@SpringBootTest` 未受调度启用影响。
- **下一步**：US2 主动查询收敛（需 `PaymentChannel.queryStatus` + Mock 四态）、US3 有限重试（需 `PaymentAttempt` 扩展 errorType/nextRetryAt）、US5 指标/告警时长（需持久化列）。US4 冻结。
- **关键环境事实**：沙箱 `./mvnw` 损坏，但 `C:\Users\user\apache-maven-3.9.5\bin\mvn.cmd` 经 **PowerShell** 可正常构建（JDK 25 / Maven 3.9.5 / 离线 `-o` 可用）。Bash 调 `cmd /c` 被安全策略阻断，必须用 PowerShell 工具。

## 咨询：项目最终标准 / 验证与观测 / 面试亮点（2026-08-28）
- 标准：Constitution 八性（Production-Oriented）；Phase 0→10，当前 001/003 已验收，**004-ledger 已前置**（原延后 Phase 8，消解 D1 矛盾，Constitution 升 v2.1.0）；终点=资金可追溯（复式记账）、未知可收敛、差异可解释、演进有证据。
- 核心难点：六条领域边界（Order≠Payment、Payment≠Channel、支付成功≠权益授予、对账≠结算、退款≠调渠道接口、履约不耦合支付）+ 七条一致性必答（幂等键/显式状态机/最终一致/重试退避上限/重复乱序回调/超时≠失败/UNKNOWN 不猜成败）。
- 验证分层：mvn compile+test（已过，60+ 测试类）→ 针对性异常场景（幂等重放、TIMEOUT→UNKNOWN、重复回调只触发一次履约、乐观锁 CONFLICT、退款累计不超限）→ 端到端（需 Docker 起 MySQL 后走 Swagger 全链路）。
- 观测：/actuator/health、/actuator/prometheus、Swagger UI、Prometheus+Grafana（docker-compose）、结构化审计日志（traceId/paymentId 关联）、业务指标（成功率/超时率/UNKNOWN 堆积）。
- 面试亮点：领域边界与依赖方向、幂等+状态机吸收重复回调、long 分+Money VO+乐观锁、ArchUnit 边界测试与契约测试、ADR 治理与人类决策边界、「不做什么」的克制（不引 MQ/K8s/2PC，基础设施决策门槛 5 问）。
- 诚实短板（面试要主动说）：MVP 无真实资金记账（待 004-ledger）、安全在 Phase 9；technical-solution.md(08-26) 部分滞后于 roadmap（称 refund/recon/settlement 为骨架、Ledger 延后 Phase 8），以 roadmap Current Status + constitution v2.1.0 为准。

## Phase 2/3/4 收尾（Redis 基建 · Stock 聚合 · 超时/限流/缓存/秒杀）
- **编译/测试调用固定为 PowerShell + `mvn.cmd`**：`cd C:\Users\user\Desktop\GoProj\PaymentArch; & 'C:\Users\user\apache-maven-3.9.5\bin\mvn.cmd' -o test '-pl' <svc> '-am' '-DfailIfNoTests=false' '-Dsurefire.failIfNoSpecifiedTests=false'`。Bash 的 `&`/Unix `mvn` 均不可用；`-D` 参数须加单引号防 PowerShell 吞掉。
- **Phase 2（012 下单幂等）**：order-service 测试修复——`OrderEntryIdempotencyServiceTest` 误用 `Optional.contains`（相等语义而非子串，改 `.orElse("").contains(...)`）；`OrderControllerIdempotencyTest` 由 `@WebMvcTest` 改为 standalone MockMvc（主类 `@MapperScan` 触发 MyBatis Mapper 需 SqlSessionFactory，web 切片缺它）。
- **Phase 3（013 Stock 聚合）**：catalog 新增 `Stock`/`StockReservation` 聚合 + 三段式（reserve→confirm→release，不变量 `total=available+reserved+sold`）；order-service `OrderApplicationService` 6 参构造注入 `OrderTimeoutScheduler`，下单预占、支付成功确认扣减、失败/超时释放；demo/reset.sh 为 3 个 SKU 预置库存。
- **Phase 4（014）全部绿**：
  - order-service：`OrderTimeoutScheduler`（Redis ZSet 时间轮，score=到期时间戳）+ `RateLimiter`（固定窗口 429 快速失败、不返回 Retry-After，owner 决策「拒绝不允许重试」）+ `RateLimitInterceptor`。修复：`RateLimiter` 加 `@Component` 注册为 bean；测试用 `lenient()` / `anyDouble()` 修正 stub。order-service **36 测试 BUILD SUCCESS**。
  - catalog-service：加 `spring-boot-starter-data-redis`；`SkuCache`（cache-aside，开关 `catalog.cache.enabled`，fail-open）；`SeckillStockService` + `seckill-deduct.lua`（`EXISTS`→-2 放行 / -1 不足 / DECRBY 扣减）+ `SeckillStockController`（`/internal/stock/seckill/{seed,deduct,rollback}`）+ `CatalogRedisConfig`（绑定 `CatalogCacheProperties`/`SeckillProperties` 并注册 Lua `RedisScript<Long>`）。catalog **34 测试 BUILD SUCCESS**。
  - order-service 秒杀端到端：新增 `SeckillResult`/`SeckillDeductResponse`、`CatalogClient.trySeckillDeduct/rollbackSeckill`、Feign 映射，`doCreateOrder` 下单前 Lua 预扣（不足→CONFLICT 回滚，含 seckill rollback）、`releaseStockForOrder` 回补配额；`FakeCatalogClient` 加 `seedSeckill` + 新测试 `seckillDeniesWhenQuotaExhausted`。
  - k6：`performance/catalog-seckill-k6.js`（sku_cache_read + seckill_flash 两场景，阈值 p95<50ms/错误率<1%）+ `performance/README.md`（计划/SLO/对照实验/示例结果表待实跑）。demo/reset.sh 为 SKU-103 播种秒杀配额 10。
- **待办（Phase 5，#28）**：spec 四件套（011-014，011 已存在）+ ADR 集合文档 + roadmap/runbook 同步 + 全量 `mvn -o verify -fae`。注意：spec 011 引用了 ADR-0044/0048/0052/0050 等，但 `docs/adr/` 实际仅 0001-0013，存在文档漂移，Phase 5 需核对并补齐/纠正。
