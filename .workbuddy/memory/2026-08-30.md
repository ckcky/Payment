# 2026-08-30

## 001-007 主线构建红线修复（提交 314351d）

- 起点：`git status` 干净、log 显示 001-007 已合并，但 `mvn -o verify` 实际是 **RED**（此前「全绿」是假象）。
- 必须用 `-fae`（fail-at-end）：Maven reactor 遇失败即中止，会掩盖后续模块问题。首轮只看到 refund-service 失败，加 `-fae` 后又暴露 reconciliation（编译错）与 ledger（测试错）。
- 五处修复，**每一处都是「测试写错了」而不是「实现错了」**，判定依据一律回到 spec/ADR：
  1. `RefundApplicationServiceTest.cumulativeUses...`：`createRefund` 是同步的，永远不返回 PROCESSING。改为让第二笔走 UNKNOWN 在途，断言 `isNotEqualTo(REJECTED)`，保留「累计额」原意。
  2. `RefundScenarioTest.postProcessFailureDoesNotRollBackRefundSuccess`：orchestrator 按 ADR-0017 同步重试 3 次，fake 记到 3 条。改断言 `hasSize(3)` + 存在 ENTITLEMENT/FAILED 尝试记录。
  3. `MybatisReconciliationRepository`：用了 `ChannelStatementSource` 却没 import → 编译失败。补 import。
  4. `LedgerTestSupport.paymentCapture`：fee=0 时仍产出 0 金额 FEE 分录，撞 `LedgerEntry` 的 `amountMinor > 0` 校验。对齐生产 `FeignLedgerPostingGateway` 的 `if (fee > 0)` 守卫。
  5. `ReconciliationBatchStateMachineTest.hasDifferenceToProcessingToClosed`：与同文件的 `closeWithUnresolvedDifferencesIsRejected` 自相矛盾。以 ADR-0019 关闭门禁为准，close 前先 `resolve(...)`。
- 结论：全 13 模块 BUILD SUCCESS。

## Phase 9 / 009-risk-security 实现（提交 6432209）

- 范围：渠道回调 HMAC 验签 + 防重放、`/internal/**` 内部鉴权、密钥 env 注入 + 脱敏、最小风控（只观测不拦截）。ADR-0024~0028 合并在 `docs/adr/0009-risk-security-decisions.md`（用户要求：四五个 ADR 放一个文档，不要一个 ADR 一个文档）。
- **关键坑 1（务必记住）**：Spring Boot 的 MockMvc **只收集 `FilterRegistrationBean`**；把 `OncePerRequestFilter` 仅标 `@Component` 会导致集成测试绕过过滤器 → 「测试全绿、生产裸奔」的假绿。安全过滤器一律在 `WebConfig` 里用 `FilterRegistrationBean` 注册。
- **关键坑 2**：Servlet url pattern 不支持中段通配（`/internal/payments/*/channel-callback` 会被当精确匹配，永不命中）。注册用前缀 `/internal/payments/*`，具体路径在过滤器内用 `AntPathMatcher` 判定。
- **关键坑 3**：过滤器读完原始 body 后必须换 `CachedBodyHttpServletRequest` 包装器，否则下游 `@RequestBody` 读到空流。
- 验签失败用 `@MockBean PaymentCallbackService` + `verify(..., never())` 直接观测「业务处理没被调用」，比靠支付状态间接推断更硬。
- `BusinessMetrics` 有两个抽象方法，**不是函数式接口**，不能写 lambda，要用 `NoopBusinessMetrics`。

## 环境事实（沿用）

- 沙箱 `./mvnw` 损坏；构建必须用 **PowerShell 工具**跑 `& 'C:\Users\user\apache-maven-3.9.5\bin\mvn.cmd' -o verify -fae`（离线）。Bash 调 cmd 被安全策略阻断。
- Maven 日志是 UTF-16 带空字节，读取时必须先 `tr -d '\000'` 再 grep，否则什么都搜不到。
- 全局异常处理在 `common/common-core/.../error/GlobalExceptionHandler.java`（`BizException` 本身无 `@ResponseStatus`，由它统一转成 `{"code":...,"message":...}`）。
- `Payment.rehydrate(...)` 是持久化重建工厂（12 参，末尾 `version` 可为 null）；`PaymentStatus` 初始态是 `PENDING`。
- 根目录散落历史临时文件（probe*/status*/pay-direct.txt 等），`git add -A` 前留意。
